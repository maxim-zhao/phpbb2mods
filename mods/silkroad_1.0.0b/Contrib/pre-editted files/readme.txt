These files have been pre-editted from a clean install of phpBB 2.0.21.
Only use them if you have no other MODS installed.

A copy of ALL the files are in this folder, even the standalone ones.

Simply copy all the files in this folder to your server and overwrite the existing ones (Always backup your original files first).
Then run the SQL commands from the .mod file.